# Production — généré depuis pyproject.toml
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
eurocodepy>=2026.4.1
jinja2>=3.1.0
fpdf2>=2.8.0

# Développement
pytest>=8.3.0
pytest-cov>=6.0.0
httpx>=0.28.0
ruff>=0.8.0

# Capacity-based design (StructuralCodes) — requiert triangle (pas de wheel ARM64)
structuralcodes>=0.6.0
