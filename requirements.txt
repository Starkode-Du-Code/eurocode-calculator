# Production — généré depuis pyproject.toml
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
structuralcodes>=0.6.0
# eurocodepy retiré du core — installable via `pip install -e ".[eurocode]"` si besoin

# Développement
pytest>=8.3.0
pytest-cov>=6.0.0
httpx>=0.28.0
ruff>=0.8.0
